import styled from "styled-components";

const IconStyled = styled.svg`
    display: block;
    width: 1em;
    height: 1em;
    margin: 0 auto;
    fill: currentColor;
`;

export {
    IconStyled, 
} 